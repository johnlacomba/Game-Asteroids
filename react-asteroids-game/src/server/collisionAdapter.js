// Simple adapter to load the collision functions from collision.js

// Get the functions from collision.js
const collisionFunctions = require('../core/collision');

// Export the collision functions
module.exports = collisionFunctions;